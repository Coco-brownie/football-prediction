# 验证体系规范：全量测试 vs Walk Forward 严格区分

## 一、核心原则
**Walk Forward（WF）验证是金标准，绝对禁止任何形式的未来函数泄露。**
全量测试仅用于快速筛选和上限参考，其结果一律不得作为最终结论。

---

## 二、全量测试（Full Training）
### 定位
- 辅助筛选工具，用于快速粗筛参数、探索方向
- 结果偏乐观，仅作为理论上限参考
- 绝对不能作为最终结论

### 允许使用的特征
- ✅ 所有特征（包括可能有泄露风险的特征）
- ✅ 全量数据训练

### 限制
- ❌ 不得用于最终结论
- ❌ 不得用于策略收益/破产率的正式结论
- ❌ 必须明确标注"全量测试，含未来函数，仅供参考"

---

## 三、Walk Forward 验证（WF）
### 定位
- 金标准验证，所有正式结论必须以WF为准
- 严格排除未来函数，模拟真实预测场景
- 策略上线、收益评估、破产率计算，一律以WF结果为准

### 严格禁止的特征（绝对不能用）
以下特征存在未来函数泄露风险，WF验证时必须排除：
1. ❌ 概率衍生特征（prob_ratio_ha、prob_draw_share、prob_max、prob_entropy、prob_home_favorite）
   - 注意：经实测，当前版本的概率特征无明显泄露，但为了安全起见，WF验证时仍建议排除
2. ❌ 任何用全量数据计算的特征
3. ❌ 任何包含未来信息的特征

### 必须遵守的规则
1. **滚动训练**：只用过去的数据训练，预测未来，绝对不能用未来的数据
2. **特征隔离**：训练集和测试集的特征必须严格分开，不能有任何交叉
3. **参数隔离**：调参只能用训练集，不能用测试集
4. **结果标注**：所有WF结果必须明确标注"Walk Forward验证，无未来函数"

---

## 四、验证等级体系
### Level 1：全量回测（最低可信度）
- 全量数据训练，全量数据测试
- 用途：快速筛选、方向探索
- 可信度：⭐（严重偏乐观）

### Level 2：赛季重置回测（中等可信度）
- 全量数据训练，按赛季重置资金
- 用途：策略稳定性初步评估
- 可信度：⭐⭐⭐（仍有未来函数）

### Level 3：Walk Forward 赛季重置（最高可信度）
- 滚动训练，无未来函数，赛季重置资金
- 用途：最终结论、策略上线评估
- 可信度：⭐⭐⭐⭐⭐（金标准）

---

## 五、执行规范
1. 任何新策略/新参数，必须先过Level 1筛选，再过Level 3验证
2. 正式报告/结论，必须以Level 3结果为准
3. 所有结果文件必须明确标注验证等级
4. 发现特征泄露，立即降级处理，并重新验证

---

## 六、当前验证状态（v0.7.1）
- ✅ 全量基准版（Level 1）：已完成，36组策略全部正收益，最高夏普3.0
  - ⚠️ 严重偏乐观，仅供快速筛选，不能作为最终结论
- ✅ WF赛季重置版（Level 3）：已完成，金标准结果
  - 只有3个策略正收益（8.3%），最高夏普0.09，最高ROI+1.2%
  - 模型真实alpha只有1-2%，还不够覆盖庄家抽水
  - 安全边际非常重要：20%边际勉强打平，10%亏20-30%，无安全边际亏30-60%
  - 质量远大于数量：出手越少，亏得越少

### 核心教训
1. **全量回测都是骗人的**：看起来很美，实际差几百倍
2. **特征泄露防不胜防**：两层泄露，第一层很明显，第二层很隐蔽
3. **WF验证是底线**：任何策略上线前必须过WF验证
4. **足球预测很难**：没有圣杯，模型alpha有限，符合市场规律
